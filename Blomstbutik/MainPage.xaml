<Page
    x:Class="Blomstbutik.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Blomstbutik"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ListView
       ItemsSource ="{x:Bind BlomstViewModel.OC_Blomst}"
        Margin="516,303,425,311" SelectionChanged="ListView_SelectionChanged">
    <ListView.ItemTemplate>

            <DataTemplate x:DataType="local:Ordreblomst">

                <StackPanel Orientation="Horizontal">

                        <TextBox Text="{x:Bind Navn, Mode=TwoWay}"/>

                        <TextBlock Text="{x:Bind Antal, Mode=TwoWay}"/>

                        <TextBlock Text="{x:Bind Farve,Mode=TwoWay}"/>

                    </StackPanel>

                </DataTemplate>

            </ListView.ItemTemplate>
    </ListView>

</Page>
